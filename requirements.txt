# This file lists the dependencies for the project. You can install them using pip:
# pip install -r requirements.txt
#or conda:
# conda install --file requirements.txt
# Make sure to have Python 3.8 or higher installed.
pytorch>=2.1
transformers>=4.42
tokenizers>=0.19
datasets>=2.19
accelerate>=0.30
safetensors>=0.4
huggingface_hub>=0.23
numpy
tqdm
matplotlib
