# This file lists the dependencies for the project. You can install them using pip or conda.
# pip: pip install -r requirements.txt; if using pip switch pytorch to torch
# conda: conda install -c conda-forge --file requirements.txt
# Make sure to have Python 3.8 or higher installed.
pytorch>=2.1
transformers>=4.42
tokenizers>=0.19
datasets>=2.19
accelerate>=0.30
safetensors>=0.4
huggingface_hub>=0.23
numpy
tqdm
pyyaml
matplotlib
# TOFU finetuning
peft>=0.11
bitsandbytes>=0.43  # optional, for 4/8-bit base model loading on tight VRAM
